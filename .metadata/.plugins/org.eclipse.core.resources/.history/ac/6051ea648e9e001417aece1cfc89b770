//Ryan Backa
//VFW1501
//Project 2

var mainWindow = Ti.UI.createWindow({
	backgroundColor: "fff",
	backgroundRepeat: true,
	title: "Well Testing Equipment"
});

var titleView = Ti.UI.createView({
	top: 20,
	height: 40,
	backgroundColor:"#848484"
});

var loadFile = require("JSON");

var headTitle = Ti.UI.createLabel({
	text: loadFile.data.equipNames.headTitle,
	font: {fontSize: 14, fontFamily: "Arial"},
	color: "#fff",
	center: titleView
});

console.log(loadFile.data.termsTest.length);

//for(var i=0; i<loadFile.data.termsTest.length; i++ ){
//var equipTable = Ti.UI.createTableView({
	//data: loadFile.data.termsTest[i].equip,
	//top:titleView+5
//});
//mainWindow.add(equipTable);
//};

titleView.add(headTitle);
mainWindow.add(titleView, loadFile.data);
mainWindow.open();